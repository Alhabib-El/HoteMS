VITE_API_URL=http://localhost:4000/api

# Temporary: skips the PIN login screen entirely (grants Management access). Only for local dev
# before the database/staff seed exist. Must match backend's DISABLE_AUTH. Set to false once live.
VITE_DISABLE_AUTH=false
