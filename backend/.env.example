DATABASE_URL="postgresql://hotems:hotems@localhost:5433/hotems?schema=public"
PORT=4000
CORS_ORIGIN="http://localhost:5173"
JWT_SECRET="change-me-to-a-long-random-string"

# Temporary: skips PIN auth entirely (everyone gets Management access). Only for local dev
# before the database/staff seed exist. Set to false (or remove) once staff PINs are live.
DISABLE_AUTH=false
